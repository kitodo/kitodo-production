# Travis continuous integration for Kitodo.Production

dist: trusty

language: java

jdk:
  - oraclejdk7
  - openjdk7

addons:
  apt:
    packages:
      - ant-optional
      - tomcat7-common

sudo: required

cache:

env:

script: ant -lib /usr/share/tomcat7/lib all
